# Copy to .env and fill in to switch from the local JSON dev store to real Supabase.
# Both must be set for SupabaseReportRepository to activate (app/db/repository.py).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

ENVIRONMENT=development
CORS_ORIGINS=["http://localhost:3000"]
